# September 10 presentation update — review banners hidden

The user explicitly requested removing the two yellow review banners across
Private GIS, Public GIS, and Storymap and updating the four existing PRs.
This is presentation approval, not resolution of EPA-2 or other geometry issues.
At the initial handoff, all PRs remained draft/unmerged pending review. The user
subsequently authorized merging and aligning `main` with the already-live sites;
see `../../ops/releases/2026-09-10-main-sync.md`. This later merge authorization
does not turn the presentation approval into GIS signoff.

**Deployed and verified.** All 81 currently referenced files across the three
sites match the release over HTTPS. Browser checks covered B–B in both modes,
all three analytes, and both coordinate options on all three sites (36
combinations): both banners stay hidden, the comparison remains available, and
no browser errors or warnings were captured. All 36 HTML URLs retain the existing
cache policy, including 304 responses; all three site entry points return 200.

## Exact scope

- Added `--hide-review-banners`, allowed only with `--production-review`.
- A body attribute and one narrowly scoped CSS rule hide `.preview` and
  `#warning`, including when analyte/mode/coordinate controls update the page.
- All JSON data, original diagram images, application JavaScript, and plotting
  code remain byte-identical to the September 9 release. Comparison control,
  table position checks, marker positions, history, and default geometry remain.
- Each site changes only its 12 HTML pages and adds one hashed stylesheet.
  Previous styles/assets are retained for cached shells and rollback.

## Build and validation

Build with the same verified source ZIPs using `--production-review
--hide-review-banners`. Build identifier: `production-20260910T0021Z`.
Ten Python/eight JavaScript tests pass. Full source reconciliation and the
558-selection/1,256-position checks pass unchanged. Banner visibility does not
change `geometryApproved: false` or the unresolved review status.

Before deployment, each generated HTML file is checked to differ from the prior
release only in the stylesheet reference and banner-visibility attribute. The
stylesheet adds exactly one visibility rule. All 14 data/image/code assets
retain their original content hashes.

## Deployment and rollback

Targeted release directory:
`/var/lib/nuglobal-releases/20260910T0021Z-banner-removal/`.
The previous HTML files are saved under `rollback/` with the three app prefixes.
SSM command `cc58eb04-4290-4037-a410-ba98e4fc1d8f` completed successfully.
All other pre-existing files were verified unchanged by inode, size, modification
time, and SHA-256: Private 240 files, Public 293, Storymap 289. Nginx configuration
hashes are unchanged, and no reload/restart was needed.
Rollback restores those HTML files atomically; the previous stylesheet is still
present. Nginx, authentication, main-map files, data, and existing assets do not
need changes. Preserve the existing 36 exact transect HTML revalidation rules.

The PR descriptions record deployment and live verification status. The later
explicit merge request supersedes the initial hold; removing the presentation
banners still must not be treated as GIS signoff.
