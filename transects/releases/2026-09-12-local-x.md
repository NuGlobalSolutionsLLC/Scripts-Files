# September 12, 2026 — local horizontal-position correction

Status: unpublished fix on `fix/transect-joined-x`. The user first requested a
local correction and then authorized separate PRs for this fix, excluding the
parked login work. Deployment and merging are not authorized by the PR request.

## Change

- Default horizontal position now uses the second DBF X field, `X_1`.
- Original `exg_tos_el` / `exg_bos_el` screen elevations are unchanged. Each
  corrected tuple is formed from one raw row before deduplication, preserving
  subsegment pairings. Original and fully joined geometry remain comparisons.
- Position checks distinguish horizontal coordinates from screen intervals.
  The existing production-review/banner controls remain intact.
- Selection rules, results, dates, history, original coordinates, source-row
  provenance, background diagrams, and layout are unchanged.

## Local evidence

Build: `transects/build/review-20260912-x1/` (default local-preview mode).
Preview: `http://127.0.0.1:8767/s2bb_max.html?analyte=TCE`.

Unchanged input SHA-256 values:

- `Transects_NAD83 (1).zip`:
  `95bb87689849a6640b2e817c3d0f983bd4049aba41ebaccfe4d5daa742d722ee`
- `Transect_A_A.zip`:
  `a05b48f37c89b19e36ae38bbf8e99d60163b4b9cd9b2aa6e4f41908ae87041d6`

Validation passed:

- 14 Python and 11 JavaScript tests, including horizontal-only correction,
  per-row coordinate pairing, same-day result ties, and missing-field rejection.
- Independent source reconciliation: 14,974 raw rows and all 4,290 distinct
  measurement keys retained; all three coordinate views reconciled against
  each measurement's exact source rows. Twelve pages and 29 manifest files
  verified.
- Display-model checks: 558 well/analyte/mode selections and 1,888 positions
  across the three coordinate views, including finite dimensions and bounds.
- Browser checks: all 36 section/analyte/mode combinations load with corrected
  X selected and expected well counts; finite, positive screen dimensions;
  no captured application warnings/errors. Both mode-selector directions
  preserve the selected analyte and return to the corrected-X default.
- EPA-2 comparisons show one corrected horizontal position, three original
  horizontal positions, and one fully joined interval. Its TCE history retains
  six zero-valued measurements through 1986-05-08. AA/HM-121 retains 18 TCE
  measurements through 2025-03-11; FF/WSETAHM31R retains 41 through 2025-09-03.
- Deep comparison with the existing Public source data: removing only the new
  `joinedX` coordinates and `defaultPosition` metadata leaves all six section
  JSON files exactly equal. All six background PNGs are byte-identical.
- JavaScript syntax and whitespace checks pass.

The horizontal field changes 207 raw rows: AA 9, BB 75, CC 54, DD 15, EE 54,
FF 0. These are coordinate corrections, not added/removed measurements.

## Remaining scope

EPA-2's 60 BB source rows all use X = 7442.06 in the default view, including
records previously drawn near P-2. Its Maximum TCE view still has three vertical
screen intervals, now aligned at that X; changing to the joined elevation
fields would collapse them, but Mary has only confirmed the horizontal field.
Twelve wells retain different X values across subsegments; no arbitrary
collapse was applied. `geometryApproved` and `productionReady` remain false.

No production, authentication, map-data, server, or main-map UI changes. The
three separately parked login-fix PRs remain outside this change. Deployment
requires separate authorization; elevation-field confirmation remains pending.

## Separate PR preparation

The builder branch starts at `Scripts-Files` main `dfaf404`. Each application
branch is independently based on its freshly fetched main, not on `fix/login-retry`:

- Private: `eb01df5b088035927287076751727177db6f2c6e`; login PR #72 excluded.
- Public: `d79c8ce378e58009dde2bf475ebeae4782307a84`; login PR #59 excluded.
- Storymap: `237ed09542ec4f0a96f5140de3d7230c226465fb`; login PR #56 excluded.

PR artifact: `transects/build/pr-20260912-x1/`, built with
`--release-candidate --hide-review-banners`. The added release-candidate option
allows preserving the already approved hidden-banner presentation for an
unpublished PR without claiming production approval. The report explicitly sets
`releaseCandidate: true`, `productionReviewRequested: false`,
`productionReady: false`, and `geometryApproved: false`.

All 15 runtime/data/image assets match the locally reviewed build byte-for-byte;
the 12 HTML pages differ only in the hidden-banner attribute and unpublished
release-candidate notice. The default coordinate rule is unchanged.

PR validation:

- 15 Python + 11 JavaScript builder tests passed, including the new unpublished
  packaging guard. Full source reconciliation and 558-selection/1,888-position
  display-model checks passed again.
- Fresh lockfile installs, application tests (Private 3, Public 5, Storymap 17),
  and full production builds passed in all three isolated worktrees.
- All 27 candidate files match both each application's `public/` and fresh
  `dist/spa/` output. The 16 existing hashed assets per application are retained
  unchanged. Only transect source files are included in the application PRs;
  generated `dist/`, main-map data, login, and dependency files are excluded.
- 108 browser combinations passed: six sections, two display modes, and three
  analytes in each of the three fresh application builds. The corrected-X
  default, hidden banners, well counts, and finite positive screens were checked;
  no application console warnings/errors were captured.
- Private and Storymap lint passed. Public's existing CommonJS/ES-module lint
  configuration failure is unchanged and remains a separate backlog item.

These checks validate the PR artifacts locally, not production or GIS approval.
