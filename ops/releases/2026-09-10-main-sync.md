# September 10 source-to-production alignment

The user explicitly requested merging the four existing transect PRs to `main`
and ensuring each application's main source reproduces its live site. This
supersedes the earlier draft-PR merge hold; it does not resolve the deferred
EPA-2 positioning question or constitute GIS approval.

## Pre-merge verification

Fresh lockfile installs and full production builds succeeded for all three
application PR revisions. Every generated file was compared by SHA-256 with the
corresponding live file over the existing SSH connection:

| Application | Verified source revision | Fresh build files matching live | Retained server-only assets |
| --- | --- | --- | --- |
| Private GIS, PR 71 | `1ed06a7fd34e458b0ca7f6885a6952a3619570ea` | 253 / 253 | 0 |
| Public GIS, PR 58 | `01c5fb3689f6040cf5dfc675ea962b9cc8c8f4c6` | 274 / 274 | 32 |
| Storymap, PR 55 | `980225569d98ec7a55db2dcecfd199fa70532261` | 302 / 302 | 0 |

There were no differing or missing build files, and every tracked `public/`
source file matched its live counterpart. Public's 32 additional files are older
hashed JavaScript, CSS, and fonts retained for cached browsers. They are listed
separately in `2026-09-10-live-manifest.json` and are not removed or presented as
part of the current build.

All 10 Python and 8 JavaScript transect tests and all 5 Public application tests
passed again. Full source reconciliation and representative live browser checks
were completed for the unchanged September 10 release, as recorded in
`../../transects/releases/2026-09-10.md`.

## Tracked configuration and deployment boundary

The active nginx configuration is now captured under
`../nginx/apps_nuglobalsolutions.conf`, SHA-256:

`bc1f27179f719428204a5065bee09d808fd3d6231a4fd07f1563a74882a78573`

It matches the live file byte-for-byte. No configuration change, reload,
restart, authentication change, data rewrite, or application redeployment is
needed for this alignment: the approved source already reproduces production.
Existing release backups and cached-browser assets remain in place.

`Scripts-Files` is the offline builder/operations repository, not a fourth live
application. Its code and release evidence belong on `main`; application build
artifacts are deployed only to the three directories documented in `../README.md`.

The manifest records the precise pre-merge application source revisions. The
associated merge commits must preserve those application trees; verify their
`main` trees and the current live hashes after merging.

## Associated PRs and remaining follow-up

- [Builder and operations #1](https://github.com/NuGlobalSolutionsLLC/Scripts-Files/pull/1)
- [Private GIS #71](https://github.com/NuGlobalSolutionsLLC/AFP4_Private/pull/71)
- [Public GIS #58](https://github.com/NuGlobalSolutionsLLC/AFP04_Public/pull/58)
- [Storymap #55](https://github.com/NuGlobalSolutionsLLC/AFP04-storymap/pull/55)

The client requested that the presentation banners remain hidden while the
remaining transect issues are examined later. Preserve the current default,
comparison control, and data. Investigate EPA-2's exported-screen versus joined
coordinate mismatch with Mary/Daphne separately; no WGS 84 reprojection or
silent position correction was applied here.
